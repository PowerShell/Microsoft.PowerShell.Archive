parameters:
  - name: vmImageName
    type: string
    default: 'windows-2019'

  - name: jobName
    type: string
    default: 'run_test_windows'

  - name: jobDisplayName
    type: string
    default: 'Run test'

steps:
  - script: echo ${{ parameters.vmImageName }}